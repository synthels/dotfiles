# My dotfiles

Arch install ft. i3 (sway, waybar, fuzzel) + kitty.

## Notes

1. To get the tray clock to work, try

```
localectl set-locale LC_TIME=nb_NO.UTF-8
```

2. You should install `ttf-fira-code` and `otf-font-awesome`.
